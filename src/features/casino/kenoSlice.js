import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};

const kenoGameSlice = createSlice({
  name: "kenoGame",
  initialState,
  reducers: {
    
  },
});

export const {} = kenoGameSlice.actions;

export default kenoGameSlice.reducer;
