import { createSlice } from "@reduxjs/toolkit";

const initialState = {

};

const slideGameSlice = createSlice({
  name: "slideGame",
  initialState,
  reducers: {
    
  },
});

export const {} = slideGameSlice.actions;

export default slideGameSlice.reducer;
