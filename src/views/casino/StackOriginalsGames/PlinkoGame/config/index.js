const pins = {
  startPins: 3,
  pinSize: 2,
  pinGap: 25,
};

const ball = {
  ballSize: 7,
};

const engine = {
  engineGravity: 1.0,
};

const world = {
  width: 500,
  height: 500,
};

const colors = {
  background: "#0f212e",
};

export const config = {
  pins,
  ball,
  engine,
  world,
  colors,
};
